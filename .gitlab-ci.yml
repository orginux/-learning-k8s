stages:
  - test

test:
  image: traherom/kustomize-docker
  stage: test
  variables:
    DEPLOY_NAME: my-deploy
  script:
    - kubectl scale deployment $DEPLOY_NAME --replicas=7
  environment:
    name: staging
  tags:
    - kubernetes
